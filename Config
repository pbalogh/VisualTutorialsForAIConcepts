package.VisualTutorialsForAIConcepts = {
    interfaces = (1.0);

    build-system = npm-pretty-much;
    build-tools = {
        1.0 = {
            NodeJS = default;
            NpmPrettyMuch = 1.10;
        };
    };

    dependencies = {
        1.0 = {
            # Declare brazil packages that build package.json dependencies here
        };
    };

    test-dependencies = {
        1.0 = {
        };
    };

    allow = {
        BrazilPath = '*';
    };
};
